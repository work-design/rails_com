<%= f.fields :extra, scope: :extra, model: f.object.form_extra do |ef| %>
  <% taxon.parameters.each do |k, v| %>
    <%= render partial: "default_form/#{DefaultForm.config.mapping[v.to_sym][:input]}", locals: { f: f, ef: ef, name: k }  %>
  <% end %>
<% end %>
