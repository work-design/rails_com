<div class="inline fields" id="parameters_<%= params[:index] %>">
  <label></label>
  <%= f.fields :parameters, index: params[:index], wrap: { all: false, label: false, placeholder: true, input: 'two wide field' } do |pf| %>
    <%= pf.text_field :column %>
    <%= pf.select :value, options_for_select(RailsCom.config.mapping.keys), class: 'ui dropdown selection' %>
    <div class="field">
      <%= link_to add_item_data_lists_path(index: params[:index].to_i + 1), remote: true do %>
        <i class="fas fa-plus"></i>
      <% end %>
      <%= link_to remove_item_data_lists_path(index: params[:index].to_i), remote: true do %>
        <i class="fas fa-minus"></i>
      <% end %>
    </div>
  <% end %>
</div>
