<%= turbo_stream.replace "filter_update_#{model.id}", partial: 'filter_form_update', locals: { filter: model } %>
<%= turbo_stream.replace "filter_tag_#{model.id}", partial: 'filter_form_tag', locals: { filter: model } %>
