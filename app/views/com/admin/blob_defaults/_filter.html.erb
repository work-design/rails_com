<%= form_with theme: 'search', model: BlobDefault.new do |f| %>
  <div class="fields">
    <%= f.text_field :record_class %>
    <%= f.text_field :name %>
    <%= f.submit %>
    <%= link_to t('.clear'), filter_params(except: [:record_class, :name]), class: 'ui button' %>
  </div>
<% end %>


