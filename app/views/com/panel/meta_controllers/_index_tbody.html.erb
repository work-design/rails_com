<tr id="tr_<%= meta_controller.id %>">
  <td><%= meta_controller.namespace_identifier %></td>
  <td>
    <%= button_to({ action: 'move_higher', id: meta_controller.id }, method: :patch, class: 'has-text-grey-lighter') do %>
      <i class="fa-light fa-arrow-up"></i>
    <% end %>
    <%= button_to({ action: 'move_lower', id: meta_controller.id }, method: :patch, class: 'has-text-grey-lighter') do %>
      <i class="fa-light fa-arrow-down"></i>
    <% end %>
    <span aria-label="<%= meta_controller.identifier %>"><%= meta_controller.name %></span>
    <%= link_to({ action: 'edit', id: meta_controller.id }, aria: { label: t('.edit') }, class: 'has-text-grey-lighter') do %>
      <i class="fa-light fa-pencil-alt"></i>
    <% end %>
  </td>
  <td>
    <% meta_controller.meta_actions.each do |meta_action| %>
      <div>
        <span aria-label="<%= meta_action.action_name %>"><%= meta_action.name %></span>
        <%= link_to({ controller: 'meta_actions', action: 'edit', meta_controller_id: meta_controller.id, id: meta_action.id }, id: "roles_#{meta_action.id}", class: 'has-text-grey-lighter') do %>
          <i class="fa-light fa-users"></i>
        <% end %>
        <%= link_to({ controller: 'meta_actions', action: 'edit', meta_controller_id: meta_controller.id, id: meta_action.id }, aria: { label: t('.edit') }, class: 'has-text-grey-lighter') do %>
          <i class="fa-light fa-pencil-alt"></i>
        <% end %>
      </div>
    <% end %>
  </td>
</tr>
