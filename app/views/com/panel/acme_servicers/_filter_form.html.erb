<%= form_with theme: 'search', model: Com::AcmeServicer.new, url: { action: params[:action] } do |f| %>
  <div class="field-body">
    <%= f.text_field :domain %>
    <div class="field is-narrow">
      <%= f.submit %>
      <%= link_to t('.clear'), filter_params(except: [:domain]), class: 'button is-light' %>
    </div>
  </div>
<% end %>
