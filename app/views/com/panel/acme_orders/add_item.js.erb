<% f_object = form_object(@acme_order, can: { wrap_id: true }, origin: { label: false }) %>

document.getElementById('card_template_advances_<%= params[:index] %>').insertAdjacentHTML(
  'afterend',
  '<%= j(render partial: 'item_field', locals: { f: f_object, index: UidHelper.sec_uuid, value: nil }) %>'
)
