<%= form_with theme: 'search', model: Com::CacheList.new, url: { action: 'index' } do |f| %>
  <div class="field-body">
    <%= f.text_field :name %>
    <div class="field is-narrow">
      <%= f.submit %>
    </div>
  </div>
<% end %>
