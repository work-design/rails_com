<p id="attachment_<%= video.id %>">
  <%= link_to video.filename, rails_ext_video_path(video), target: '_blank' %>
  <%= link_to 'x', panel_attachment_path(video.id), method: :delete, data: {confirm: t('.confirm')}, remote: true %>
</p>

