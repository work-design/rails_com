<table class="table is-fullwidth is-hoverable as-show">
  <thead>
    <tr>
      <th>
      </th>
      <th>
        <%= link_to t('.edit.title'), { action: 'edit' }, data: { turbo_frame: 'modal' }, class: 'button is-small is-link' %>
      </th>
    </tr>
  </thead>
  <tbody>
    <%= render 'show_table' %>
  </tbody>
</table>
