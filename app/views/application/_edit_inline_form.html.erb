<%= form_with theme: 'inline', model: model, scope: model_name, url: { action: 'update' } do |f| %>
  <%= render partial: "inline_form/#{params[:field_type]}", locals: { f: f, name: params[:field] } %>
  <%= f.submit %>
<% end %>
