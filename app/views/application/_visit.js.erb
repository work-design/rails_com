Turbolinks.clearCache()
document.documentElement.classList.remove('is-clipped')
<% if defined? return_to %>
Turbolinks.visit('<%= return_to.html_safe %>', { action: 'replace' })
<% else %>
Turbolinks.visit(location.href, { action: 'replace' })
<% end %>
