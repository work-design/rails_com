<% if defined? model %>
  <%= turbo_stream.append 'body', partial: 'actions', layout: 'weui_actionsheet', locals: { model: model } %>
<% else %>
  <%= turbo_stream.append 'body', partial: 'actions', layout: 'weui_actionsheet', locals: { model: instance_variable_get("@#{model_name}") } %>
<% end %>
