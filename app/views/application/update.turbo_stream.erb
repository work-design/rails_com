<%= turbo_stream.replace "tr_#{params[:id]}", partial: 'index_tbody', locals: { controller_name.singularize.to_sym => instance_variable_get("@#{controller_name.singularize}") } %>
<%= turbo_stream.remove 'modal' %>
