<%= render layout: 'layouts/admin_head' do %>
  <main class="columns">
    <div class="column is-narrow p-0">
      <%= render 'nav' %>
    </div>
    <turbo-frame id="body" data-turbo-action="advance" class="column pt-0">
      <%= render 'nav_bar' %>
      <%= render 'alert' %>
      <%= yield %>
    </turbo-frame>
  </main>
<% end %>
